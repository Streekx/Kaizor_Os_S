hostname="kaizor-os"

# Core Services
dbus_enable="YES"
seatd_enable="YES"

# Networking (basic)
ifconfig_DEFAULT="DHCP"

# Enable Linux compatibility (future apps support)
linux_enable="YES"
